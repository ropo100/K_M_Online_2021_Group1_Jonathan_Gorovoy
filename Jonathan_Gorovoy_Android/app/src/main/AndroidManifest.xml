<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jonathan_gorovoy_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jonathan_Gorovoy_Android">
        <activity android:name=".ModifyRoutineActivity"></activity>
        <activity android:name=".ModifyEventActivity" />
        <activity android:name=".ModifyDayActivity" />
        <activity android:name=".PastDayActivity" />
        <activity android:name=".WeekCalendarActivity" />
        <activity android:name=".MonthCalendarActivity" />
        <activity android:name=".ViewRoutinesActivity" />
        <activity android:name=".ViewDeadlinesActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>